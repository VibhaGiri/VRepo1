package JavaSamples;

import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;

class SortArrayTest
{
    @Test
    void addition()
    {
        int[] array={3,7,9,1,0};
        SortArray sort=new SortArray();
      //  assertArrayEquals([0,1,3,7,9],sort.sortarray(array));
     //   assertArrayEquals();
     //   assertEquals(0,sort.sortarray(array));
    }
}